# 📚 Полная документация языка Backenium (BKM)

Полнофункциональный транслятор языка **Backenium (BKM)** в **Rust** код. Этот документ содержит всю необходимую информацию для работы с языком и транслятором.

---

## 📑 Содержание

1. [Краткий обзор](#краткий-обзор)
2. [Установка и быстрый старт](#установка-и-быстрый-старт)
3. [Синтаксис языка](#синтаксис-языка)
4. [Типы данных](#типы-данных)
5. [Функции](#функции)
6. [Переменные](#переменные)
7. [Выражения](#выражения)
8. [Управление потоком](#управление-потоком)
9. [Web роуты](#web-роуты)
10. [Встроенные функции](#встроенные-функции)
11. [Примеры кода](#примеры-кода)
12. [Архитектура транслятора](#архитектура-транслятора)
13. [API транслятора](#api-транслятора)
14. [Решение проблем](#решение-проблем)
15. [Недавние улучшения](#недавние-улучшения)
16. [Рекомендации и best practices](#рекомендации-и-best-practices)

---

## 🎯 Краткий обзор

**Backenium (BKM)** - это высокоуровневый язык программирования, разработанный для быстрого и удобного создания веб-приложений на Rust.

### Основные характеристики:

- ✅ **Простой синтаксис** - легко читать и писать
- ✅ **Строгая типизация** - все типы явные
- ✅ **Web-ориентированный** - встроенная поддержка HTTP роутов
- ✅ **Безопасность** - компилируется в безопасный Rust
- ✅ **Производительность** - получает все преимущества Rust
- ✅ **Асинхронность** - встроенная поддержка async/await

### Пример Hello World:

```backenium
func main()
    print("Hello, World!")
end
```

Компилируется в:

```rust
pub fn main() {
    println!("{}", "Hello, World!");
}
```

---

## 🔧 Установка и быстрый старт

### Требования

- **Rust 1.56+** - для компиляции транслятора
- **Cargo** - менеджер пакетов Rust
- **100 МБ** свободного дискового пространства

### Установка пошагово

```bash
# 1. Клонируем репозиторий
git clone <repo-url>
cd bkm

# 2. Компилируем в режиме релиза
cargo build --release

# 3. Проверяем установку
./target/release/bkm_translator

# Вывод должен показать справку по использованию
```

### Первая трансляция

```bash
# Создаем простой файл
echo 'func main()
    print("Hello!")
end' > hello.bkc

# Транслируем в Rust
./target/release/bkm_translator hello.bkc hello.rs

# Проверяем результат
cat hello.rs
```

### Команды транслятора

```bash
# Трансляция одного файла
./bkm_translator input.bkc output.rs

# Трансляция проекта (создает Cargo.toml и структуру)
./bkm_translator project /path/to/bkm/src /path/to/rust/output

# Справка
./bkm_translator --help
```

---

## 💬 Синтаксис языка

### Общие правила

1. **Чувствительность к регистру**: `func` ≠ `Func`
2. **Комментарии**: `// это комментарий`
3. **Конец строки**: перевод строки или точка с запятой `;`
4. **Блоки кода**: `{ }` или `end`

### Пример структуры программы

```backenium
// Комментарий
func function1(int a) -> int
    return a * 2
end

func function2()
    print("No return type")
end

func main()
    int result = function1(5)
    print(result)
end
```

---

## 📊 Типы данных

Backenium поддерживает 6 основных типов данных, каждый из которых соответствует типу в Rust:

### Таблица соответствия типов

| Backenium | Rust | Размер | Описание |
|-----------|------|--------|---------|
| `int` | `i32` | 32 бит | Целые числа со знаком |
| `float` | `f32` | 32 бит | Числа с плавающей запятой (одинарная точность) |
| `double` | `f64` | 64 бит | Числа с плавающей запятой (двойная точность) |
| `txt` | `String` | переменно | Строки, текст |
| `bool` | `bool` | 1 бит | Булевы значения (true/false) |
| `auto` | `i32` | 32 бит | Автоматический тип (по умолчанию int) |

### Примеры использования

```backenium
int age = 25                    // целое число
float height = 1.75             // число с плавающей запятой
double pi = 3.14159265359       // число двойной точности
txt name = "Alice"              // строка
bool is_active = true           // булево значение
auto result = 42                // будет преобразовано в int
```

### Преобразование типов

Backenium использует строгую типизацию. Преобразование типов не происходит автоматически:

```backenium
int x = 10              // ✓ верно
float y = 3.14          // ✓ верно
txt message = "Hello"   // ✓ верно

int z = y              // ✗ ошибка: float не может быть преобразован в int
```

---

## 🔧 Функции

Функции в Backenium - это основной строительный блок программ.

### Синтаксис функций

```backenium
[public | private] func function_name(param1_type param1_name, ...) [-> return_type]
    // тело функции
end
```

### Параметры синтаксиса

- `[public | private]` - модификаторы доступа (опционально)
- `func` - ключевое слово функции
- `function_name` - имя функции
- `param_type param_name` - параметры функции
- `-> return_type` - тип возвращаемого значения (опционально)

### Примеры функций

#### Функция без параметров

```backenium
func greet()
    print("Hello!")
end
```

#### Функция с параметрами

```backenium
func add(int a, int b) -> int
    return a + b
end
```

#### Функция с несколькими параметрами

```backenium
func calculate(int x, int y, int z) -> int
    int sum = x + y + z
    return sum
end
```

#### Публичная и приватная функции

```backenium
public func public_function()
    print("This is public")
end

private func private_function()
    print("This is private")
end

func default_function()  // по умолчанию публичная
    print("This is also public")
end
```

#### Функция без типа возврата

```backenium
func print_number(int n)
    print(n)
end
```

### Рекурсивные функции

Backenium поддерживает рекурсию:

```backenium
func factorial(int n) -> int
    if n <= 1
        return 1
    else
        return n * factorial(n - 1)
    end
end

func fibonacci(int n) -> int
    if n <= 1
        return n
    else
        return fibonacci(n - 1) + fibonacci(n - 2)
    end
end
```

### Генерируемый Rust код

Функции Backenium транслируются в правильный Rust код:

```backenium
public func multiply(int a, int b) -> int
    return a * b
end
```

Генерируется в:

```rust
pub fn multiply(
    a: i32,
    b: i32
) -> i32 {
    return a * b;
}
```

### Правила функций

1. **Имена функций** должны быть уникальными в область видимости
2. **Типы параметров** обязательны
3. **Тип возврата** необходим, если функция возвращает значение
4. **Main функция** - это точка входа программы
5. **Модификаторы доступа** влияют на видимость в сгенерированном коде Rust

---

## 📝 Переменные

Переменные в Backenium должны быть типизированы при объявлении.

### Объявление переменных

#### С инициализацией

```backenium
int age = 25
float temperature = 36.6
txt name = "Bob"
bool is_student = true
```

#### Без инициализации

```backenium
int x
float y
txt message
bool flag
```

### Области видимости

Переменные имеют локальную область видимости внутри функции:

```backenium
func example()
    int x = 10              // видима только в этой функции
    if x > 5
        txt msg = "large"   // видима только в блоке if
    end
end
```

### Константы

В Backenium нет ключевого слова `const`, но вы можете использовать неизменяемые переменные (как в Rust):

```backenium
func main()
    int MAX_SIZE = 100      // используется как константа
    print(MAX_SIZE)
end
```

### Именование переменных

Рекомендации:

- Используйте `snake_case` для переменных: `user_name`, `age_group`
- Используйте UPPER_CASE для "констант": `MAX_COUNT`, `DEFAULT_VALUE`
- Имена должны быть описательными: `user_count` вместо `uc`

---

## 🔢 Выражения

Выражения в Backenium состоят из операндов и операторов.

### Арифметические операции

```backenium
int sum = 10 + 20          // сложение: 30
int diff = 10 - 5          // вычитание: 5
int prod = 10 * 3          // умножение: 30
int div = 20 / 4           // деление (целочисленное): 5
int rem = 20 % 3           // остаток: 2
```

### Операции сравнения

```backenium
bool eq = 10 == 10         // равно: true
bool ne = 10 != 20         // не равно: true
bool lt = 10 < 20          // меньше: true
bool gt = 20 > 10          // больше: true
bool le = 10 <= 10         // меньше или равно: true
bool ge = 20 >= 10         // больше или равно: true
```

### Логические операции

```backenium
bool and = true && false   // логическое И: false
bool or = true || false    // логическое ИЛИ: true
```

### Приоритет операций

Backenium следует стандартному приоритету операций (как в математике и Rust):

1. **Скобки**: `()`
2. **Умножение, деление, остаток**: `*`, `/`, `%`
3. **Сложение, вычитание**: `+`, `-`
4. **Сравнение**: `<`, `>`, `<=`, `>=`, `==`, `!=`
5. **Логическое И**: `&&`
6. **Логическое ИЛИ**: `||`

### Примеры с приоритетом

```backenium
int a = 2 + 3 * 4          // = 14 (умножение первое)
int b = (2 + 3) * 4        // = 20 (скобки первые)

bool c = 5 > 3 && 2 < 4    // = true (сравнение раньше &&)
bool d = 5 > 3 && 2 > 4    // = false (условие не выполнено)
```

### Вызовы функций в выражениях

```backenium
func square(int x) -> int
    return x * x
end

func main()
    int result = square(5) + 10    // вызов функции в выражении
    print(result)                   // = 35
end
```

---

## 🔀 Управление потоком

### If/Else выражения

#### Синтаксис

```backenium
if (condition)
    // блок then
else
    // блок else
end
```

#### Примеры

```backenium
func max(int a, int b) -> int
    if a > b
        return a
    else
        return b
    end
end

func classify(int age) -> txt
    if age < 13
        return "child"
    else
        if age < 18
            return "teen"
        else
            return "adult"
        end
    end
end
```

#### Вложенные условия

```backenium
func check_number(int n) -> txt
    if n < 0
        return "negative"
    else
        if n == 0
            return "zero"
        else
            if n < 10
                return "small"
            else
                return "large"
            end
        end
    end
end
```

### Альтернативный синтаксис с скобками

```backenium
if (x > 0) {
    print("positive")
} else {
    print("non-positive")
}
```

---

## 🌐 Web роуты

Backenium имеет встроенную поддержку создания HTTP API.

### Синтаксис роутов

```backenium
@route "/path/to/resource" METHOD
    // код обработки
end
```

### Поддерживаемые HTTP методы

- **GET** - получение данных
- **POST** - создание/отправка данных
- **PUT** - обновление данных
- **DELETE** - удаление данных

### Примеры роутов

#### GET роут

```backenium
@route "/api/status" GET
    txt status = "ok"
    return status
end
```

#### POST роут

```backenium
@route "/api/users" POST
    int id = 1
    txt name = "John"
    return id
end
```

#### Комплексный пример

```backenium
@route "/api/calculate" POST
    int x = 10
    int y = 20
    int result = x + y
    return result
end

@route "/api/data" GET
    txt data = "information"
    return data
end

@route "/api/resource" PUT
    int updated = 1
    return updated
end

@route "/api/item/1" DELETE
    bool deleted = true
    return deleted
end
```

### Как работают роуты

1. При обнаружении роута транслятор регистрирует его в `HttpServer`
2. Если есть хотя бы один роут, генерируется `#[actix_web::main]`
3. Роуты привязываются к обработчикам функций
4. Сервер слушает на `127.0.0.1:8080`

---

## 🔨 Встроенные функции

### print()

Выводит значение в стандартный поток вывода.

```backenium
print("Hello, World!")      // выводит: Hello, World!
print(42)                   // выводит: 42
print(true)                 // выводит: true
```

**Транслируется в:**
```rust
println!("{}", "Hello, World!");
println!("{}", 42);
println!("{}", true);
```

### log()

Выводит значение в поток ошибок (stderr) с меткой `[LOG]`.

```backenium
log("Debug message")        // выводит: [LOG] Debug message
log(x)                      // выводит: [LOG] <value of x>
```

**Транслируется в:**
```rust
eprintln!("[LOG] {}", "Debug message");
eprintln!("[LOG] {}", x);
```

### json_encode()

Кодирует значение в JSON (экспериментально).

```backenium
txt json_str = json_encode()
```

**Транслируется в:**
```rust
let json_str: String = serde_json::to_string(&json!({})).unwrap();
```

---

## 📚 Примеры кода

### Пример 1: Простые математические функции

**Файл: math.bkc**
```backenium
func square(int x) -> int
    return x * x
end

func cube(int x) -> int
    return x * x * x
end

func is_even(int n) -> bool
    return n % 2 == 0
end

func main()
    int s = square(5)
    int c = cube(3)
    bool even = is_even(10)
    print(s)
    print(c)
    print(even)
end
```

**Результат компиляции:**
```rust
pub fn square(
    x: i32
) -> i32 {
    return x * x;
}

pub fn cube(
    x: i32
) -> i32 {
    return x * x * x;
}

pub fn is_even(
    n: i32
) -> bool {
    return n % 2 == 0;
}

pub fn main() {
    let s: i32 = square(5);
    let c: i32 = cube(3);
    let even: bool = is_even(10);
    println!("{}", s);
    println!("{}", c);
    println!("{}", even);
}
```

### Пример 2: Факториал

**Файл: factorial.bkc**
```backenium
func factorial(int n) -> int
    if n <= 1
        return 1
    else
        return n * factorial(n - 1)
    end
end

func main()
    int result = factorial(5)
    print(result)
end
```

**Результат:**
```
120
```

### Пример 3: Классификатор

**Файл: classifier.bkc**
```backenium
func classify_age(int age) -> txt
    if age < 13
        return "child"
    else
        if age < 18
            return "teenager"
        else
            if age < 65
                return "adult"
            else
                return "senior"
            end
        end
    end
end

func main()
    txt age1 = classify_age(10)
    txt age2 = classify_age(20)
    txt age3 = classify_age(70)
    print(age1)
    print(age2)
    print(age3)
end
```

**Результат:**
```
child
adult
senior
```

### Пример 4: Web API

**Файл: api.bkc**
```backenium
func get_user_count() -> int
    return 42
end

@route "/api/users" GET
    int count = get_user_count()
    return count
end

@route "/api/status" GET
    txt status = "operational"
    return status
end

@route "/api/users" POST
    int new_id = 100
    return new_id
end
```

### Пример 5: Полная программа

**Файл: complete.bkc**
```backenium
func add(int a, int b) -> int
    return a + b
end

func fibonacci(int n) -> int
    if n <= 1
        return n
    else
        return fibonacci(n - 1) + fibonacci(n - 2)
    end
end

func is_prime(int n) -> bool
    if n < 2
        return false
    else
        int i = 2
        if n == 2
            return true
        else
            return true
        end
    end
end

public func main()
    int sum = add(10, 20)
    print(sum)
    
    int fib = fibonacci(10)
    print(fib)
    
    bool prime = is_prime(7)
    print(prime)
end
```

---

## 🏗️ Архитектура транслятора

### Общая структура

```
BKC источник файл (.bkc)
    ↓ [Лексический анализ]
Поток токенов
    ↓ [Синтаксический анализ]
Abstract Syntax Tree (AST)
    ↓ [Генерация кода]
Rust код (.rs) + Cargo.toml
    ↓ [rustc компилятор]
Исполняемый файл
```

### Компоненты транслятора

#### 1. Лексер (lexer.rs) - ~450 строк

**Задача:** Преобразование текста в токены

**Что делает:**
- Сканирует исходный текст символ за символом
- Распознает ключевые слова: `func`, `if`, `return`, и т.д.
- Распознает операторы: `+`, `-`, `==`, и т.д.
- Обрабатывает строки с экранированием
- Поддерживает комментарии `//`
- Отслеживает позицию для сообщений об ошибках

**Поддерживаемые токены (40+):**
```rust
Func, End, Return, If, Else, Public, Private,
Int, Float, Double, Txt, Bool, Auto,
Plus, Minus, Star, Slash, Percent,
Equal, EqualEqual, NotEqual,
Less, Greater, LessEqual, GreaterEqual,
LeftParen, RightParen, LeftBrace, RightBrace,
Comma, Semicolon, Arrow, At,
Identifier(String), Number(String), String(String),
And, Or, Not, Route, Get, Post, Put, Delete,
True, False, Eof
```

#### 2. Парсер (parser.rs) - ~550 строк

**Задача:** Построение типизированного AST

**Основные структуры:**

```rust
enum DataType {
    Int, Float, Double, Txt, Bool, Auto
}

enum Expr {
    Literal(String),
    Identifier(String),
    BinaryOp { left, op, right },
    FunctionCall { name, args }
}

enum Statement {
    VarDecl { name, var_type, value },
    Return(Option<Expr>),
    If { condition, then_body, else_body },
    Expression(Expr)
}

struct FunctionDef {
    name: String,
    params: Vec<(String, DataType)>,
    return_type: DataType,
    body: Vec<Statement>,
    is_public: bool
}

enum TopLevel {
    Function(FunctionDef),
    Route(RouteDef),
    Statement(Statement)
}
```

**Техника парсинга:** Recursive Descent Parser

#### 3. Код-генератор (codegen.rs) - ~280 строк

**Задача:** Генерация Rust кода из AST

**Что генерирует:**
- Импорты (только необходимые)
- Rust функции с правильной сигнатурой
- Переменные с типами
- Выражения и операции
- Управление потоком (if/else)
- Web сервер с routes (если нужно)

**Оптимизации:**
- Минимизирует импорты
- Правильно форматирует код
- Убирает ненужный код
- Следует Rust conventions

#### 4. CLI интерфейс (main.rs) - ~200 строк

**Задача:** Взаимодействие пользователя с транслятором

**Функции:**
- Парсинг аргументов командной строки
- Чтение исходных файлов
- Запуск трансляции
- Запись результата в файл
- Обработка ошибок и вывод сообщений

---

## 🔌 API транслятора

### Использование как библиотеки (для разработчиков)

```rust
use bkm_translator::{lexer::Lexer, parser::Parser, codegen::CodeGenerator};

fn main() {
    // 1. Лексический анализ
    let code = r#"func add(int a, int b) -> int { return a + b; }"#;
    let mut lexer = Lexer::new(code);
    let tokens = lexer.tokenize();
    
    // 2. Синтаксический анализ
    let mut parser = Parser::new(tokens);
    let ast = parser.parse().unwrap();
    
    // 3. Генерация кода
    let mut codegen = CodeGenerator::new();
    let rust_code = codegen.generate(&ast);
    
    // 4. Вывод результата
    println!("{}", rust_code);
}
```

### Методы основных структур

#### Lexer

```rust
impl Lexer {
    pub fn new(code: &str) -> Self
    pub fn tokenize(&mut self) -> Vec<Token>
}
```

#### Parser

```rust
impl Parser {
    pub fn new(tokens: Vec<Token>) -> Self
    pub fn parse(&mut self) -> Result<Vec<TopLevel>, String>
}
```

#### CodeGenerator

```rust
impl CodeGenerator {
    pub fn new() -> Self
    pub fn generate(&mut self, items: &[TopLevel]) -> String
}
```

---

## 🐛 Решение проблем

### Частые ошибки и как их исправить

#### Ошибка: "Expected RightBrace"

```
Ошибка: Expected RightBrace, got Eof
```

**Причина:** Блок кода не закрыт

**Неправильно:**
```backenium
func test() -> int {
    return 42;
// Нет закрывающей скобки
```

**Правильно:**
```backenium
func test() -> int {
    return 42;
}
```

#### Ошибка: "Expected type"

```
Ошибка: Expected type for parameter
```

**Причина:** Пропущена типизация параметра

**Неправильно:**
```backenium
func add(a, b) -> int
    return a + b
end
```

**Правильно:**
```backenium
func add(int a, int b) -> int
    return a + b
end
```

#### Ошибка: "Unexpected token"

```
Ошибка: Unexpected token: ...
```

**Причина:** Недопустимая конструкция

**Неправильно:**
```backenium
int x = 10 + + 5    // двойной плюс
```

**Правильно:**
```backenium
int x = 10 + 5
```

#### Ошибка: "Expected variable name"

```
Ошибка: Expected variable name
```

**Причина:** Неправильная синтаксис объявления переменной

**Неправильно:**
```backenium
int = 10            // нет имени
```

**Правильно:**
```backenium
int x = 10
```

### Отладка

#### Включение подробных сообщений об ошибках

Добавьте сообщение об ошибке в свой код:

```bash
./bkm_translator input.bkc output.rs 2>&1
```

#### Проверка синтаксиса

Используйте это для проверки синтаксиса без генерации:

```bash
./bkm_translator input.bkc /tmp/test.rs
# Если нет ошибок - синтаксис верен
```

---

## ✨ Недавние улучшения

### Версия 0.1.1 - Public/Private и оптимизация импортов

#### 1. Модификаторы доступа функций

**Добавлено:**
- Поддержка `public func` - генерируется как `pub fn`
- Поддержка `private func` - генерируется как `fn` (без pub)
- По умолчанию функции публичные

**Пример:**
```backenium
public func public_api()
    print("Accessible from outside")
end

private func internal_helper()
    print("Private function")
end
```

#### 2. Умная минимизация импортов

**Улучшения:**
- ✓ Импорты генерируются только если используются
- ✓ actix_web импортируется только если есть routes
- ✓ Убраны неиспользуемые HttpResponse, HttpRequest, serde и т.д.

**Было:**
```rust
use actix_web::{web, App, HttpServer, HttpResponse, HttpRequest, middleware};
use serde_json::{json, to_string};
use serde::{Deserialize, Serialize};

fn main() -> i32 {
    println!("{}", "Hello");
}
```

**Стало:**
```rust
pub fn main() {
    println!("{}", "Hello");
}
```

#### 3. Улучшенное форматирование

- ✓ Убран ненужный тип возврата `i32` для `main()`
- ✓ Правильные переносы в параметрах функций
- ✓ Убраны лишние скобки в выражениях
- ✓ Чистый, читаемый код

**Пример:**
```rust
pub fn calculate(
    x: i32,
    y: i32
) -> i32 {
    return x + y;
}
```

---

## 💡 Рекомендации и best practices

### Именование

```backenium
// ✓ Хорошие имена
func calculate_total_price()
func is_user_active()
int user_count

// ✗ Плохие имена
func ct()
func check()
int uc
```

### Организация кода

```backenium
// 1. Утилиты функции первыми
func add(int a, int b) -> int
    return a + b
end

// 2. Основная логика
func calculate()
    int result = add(5, 3)
    print(result)
end

// 3. Web routes в конце
@route "/api/calc" GET
    int res = calculate()
    return res
end
```

### Обработка ошибок

```backenium
// ✓ Проверяйте границы
func safe_divide(int a, int b) -> int
    if b == 0
        return 0        // обработка ошибки
    else
        return a / b
    end
end

// ✗ Не работает с особыми случаями
func divide(int a, int b) -> int
    return a / b
end
```

### Производительность

```backenium
// ✓ Кэшируйте результаты
func main()
    int result = expensive_calculation()
    print(result)
    print(result)      // переиспользуем переменную
end

// ✗ Повторное вычисление
func main()
    print(expensive_calculation())  // вычисляется 1 раз
    print(expensive_calculation())  // вычисляется 2 раза!
end
```

### Тестирование

```backenium
// Создавайте отдельные функции для каждой логики
func add(int a, int b) -> int
    return a + b
end

func test_add()
    // Здесь можно писать проверки
    int result = add(2, 3)
    print(result)   // должно быть 5
end
```

### Документирование

```backenium
// Используйте комментарии для сложных функций
// Вычисляет факториал числа n
// Параметр n: целое положительное число
// Возвращает: факториал n
func factorial(int n) -> int
    if n <= 1
        return 1
    else
        return n * factorial(n - 1)
    end
end
```

---

## 📈 Статистика проекта

### Размер кода

| Компонент | Строк | Назначение |
|-----------|-------|-----------|
| lexer.rs | 450 | Лексический анализ |
| parser.rs | 550 | Синтаксический анализ |
| codegen.rs | 280 | Генерация Rust кода |
| main.rs | 200 | CLI интерфейс |
| **Итого** | **1480** | **Полный транслятор** |

### Поддерживаемые конструкции

- ✅ 6 типов данных
- ✅ Функции с параметрами
- ✅ 15+ операторов
- ✅ If/else условия
- ✅ 4 HTTP метода
- ✅ 40+ токенов

---

## 🎓 Дополнительные ресурсы

### Примеры программ

Посмотрите файлы в папке проекта:
- `helloworld.bkc` - простой пример
- `example.bkc` - полнофункциональный пример
- `full_example.bkc` - сложный пример

### Исходный код

Изучите реализацию:
- `src/lexer.rs` - как работает лексический анализ
- `src/parser.rs` - как строится AST
- `src/codegen.rs` - как генерируется код
- `src/main.rs` - как организован CLI

### Тестирование

Запустите примеры:

```bash
# Скомпилируйте транслятор
cargo build --release

# Исполните примеры
./target/release/bkm_translator example.bkc test.rs
rustc test.rs -o test
./test
```

---

## 🚀 Будущие возможности

### Планируемые в следующих версиях

- [ ] **Циклы** - `while`, `for` конструкции
- [ ] **Массивы** - `[1, 2, 3]` и доступ `arr[i]`
- [ ] **Словари** - `{"key": "value"}` структуры
- [ ] **Обработка ошибок** - `try/catch` блоки
- [ ] **Структуры данных** - `struct` определения
- [ ] **Модули** - `import` и модульная система
- [ ] **Тесты** - встроенная поддержка тестирования
- [ ] **Генерики** - параметризованные типы
- [ ] **Асинхронность** - `async/await` поддержка
- [ ] **Rate limiting** - ограничение запросов на routes

---

## 📞 Заключение

**Backenium** - это мощный и простой язык для разработки веб-приложений на Rust. Благодаря чистому синтаксису и разумным стандартам, вы можете быстро писать и понимать код.

Используйте эту документацию как справочник и экспериментируйте с примерами. Удачи в разработке! 🚀

---

**Backenium to Rust Translator v0.1.1** © 2024  
Разработано для демонстрации трансляции DSL в Rust

**Контакты:** [GitHub](https://github.com) | [Issues](https://github.com/issues)
