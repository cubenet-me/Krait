# 📄 Справочник по файлам проекта

## Структура src/

```
src/
├── lib.rs                  - Публичная библиотека (точка входа для использования как lib)
├── main.rs                 - Главное приложение (CLI интерфейс)
└── modules/
    ├── mod.rs              - Экспорт всех модулей
    ├── lexer/
    │   └── mod.rs          - Лексический анализ
    ├── parser/
    │   └── mod.rs          - Синтаксический анализ и AST
    ├── codegen/
    │   └── mod.rs          - Генерация Rust кода
    ├── cli/
    │   └── mod.rs          - Интерфейс командной строки
    └── api/
        └── mod.rs          - Web API поддержка
```

## Каждый файл

### src/lib.rs (30 строк)

**Роль:** Точка входа библиотеки

**Экспортирует:**
- `translate()` - главная функция
- `Lexer, Token, TokenType` - типы лексера
- `Parser, DataType, Expr, Statement, FunctionDef, RouteDef, TopLevel` - типы парсера
- `CodeGenerator` - генератор
- `VERSION` - версия

**Использовать:**
```rust
use backenium_translator::translate;
let rust = translate("func test() end")?;
```

### src/main.rs (45 строк)

**Роль:** CLI приложение

**Что делает:**
- Парсит аргументы командной строки
- Вызывает CLI функции
- Показывает ошибки/успехи

**Зависит от:**
- `modules::cli`

**Использовать:**
```bash
./bkm_translator input.bkc output.rs
./bkm_translator project src out
./bkm_translator --help
```

### src/modules/mod.rs (5 строк)

**Роль:** Центральная точка экспорта модулей

**Содержит:**
```rust
pub mod lexer;
pub mod parser;
pub mod codegen;
pub mod cli;
pub mod api;
```

### src/modules/lexer/mod.rs (450 строк)

**Роль:** Лексический анализ

**Основные типы:**
- `Lexer` - структура лексера
- `Token` - токен
- `TokenType` - тип токена (enum)

**Основные методы:**
- `Lexer::new(input: &str)` - создать лексер
- `lexer.tokenize()` - получить токены

**Использовать:**
```rust
use backenium_translator::Lexer;
let mut lexer = Lexer::new("func test() end");
let tokens = lexer.tokenize();
```

### src/modules/parser/mod.rs (550 строк)

**Роль:** Синтаксический анализ

**Основные типы:**
- `Parser` - парсер
- `DataType` - тип данных (int, float и т.д.)
- `Expr` - выражение
- `Statement` - оператор
- `FunctionDef` - определение функции
- `RouteDef` - определение маршрута
- `TopLevel` - элементы верхнего уровня

**Основные методы:**
- `Parser::new(tokens)` - создать парсер
- `parser.parse()` - получить AST

**Использовать:**
```rust
use backenium_translator::Parser;
let mut parser = Parser::new(tokens);
let ast = parser.parse()?;
```

### src/modules/codegen/mod.rs (300 строк)

**Роль:** Генерация Rust кода

**Основные типы:**
- `CodeGenerator` - генератор

**Основные методы:**
- `CodeGenerator::new()` - создать генератор
- `generator.generate(&ast)` - получить Rust код

**Использовать:**
```rust
use backenium_translator::modules::codegen::CodeGenerator;
let mut gen = CodeGenerator::new();
let rust = gen.generate(&ast);
```

### src/modules/cli/mod.rs (150 строк)

**Роль:** Интерфейс командной строки

**Основные типы:**
- `CliResult` - результат операции (Success/Error)

**Основные функции:**
- `translate_file(input, output)` - трансляция одного файла
- `translate_project(input_dir, output_dir)` - трансляция проекта
- `show_help()` - показать справку
- `show_version()` - показать версию

**Использовать:**
```rust
use backenium_translator::modules::cli;
cli::translate_file("in.bkc", "out.rs").print();
```

### src/modules/api/mod.rs (150 строк)

**Роль:** Поддержка Web API

**Основные типы:**
- `ApiInfo` - информация об API
- `ApiConfig` - конфигурация сервера

**Основные функции:**
- `ApiInfo::from_routes()` - анализ маршрутов
- `validate_routes()` - валидация маршрутов
- `print_api_stats()` - статистика API

**Использовать:**
```rust
use backenium_translator::modules::api;
let info = api::ApiInfo::from_routes(&routes);
```

## Документация

### COMPLETE_DOCUMENTATION.md

**Что:** Полная документация языка Backenium

**Для:** Пользователей языка

**Содержит:**
- Синтаксис
- Типы данных
- Функции
- Примеры
- Архитектуру транслятора

### MODULAR_ARCHITECTURE.md

**Что:** Описание модульной архитектуры

**Для:** Разработчиков, хотящих понять структуру

**Содержит:**
- Структура проекта
- Описание каждого модуля
- Как добавить новый модуль

### ARCHITECTURE_GUIDE.md

**Что:** Гайд по разработке расширений

**Для:** Разработчиков, хотящих расширить

**Содержит:**
- Примеры расширений
- Правила модульного дизайна
- Советы и трюки

### VERSION_0_1_3.md

**Что:** История и информация v0.1.3

**Для:** Кто хочет знать что изменилось

**Содержит:**
- Исправления
- Примеры генерации
- Планы на будущее

### IMPORTS_SYSTEM.md

**Что:** Описание системы импортов

**Для:** Пользователей языка

**Содержит:**
- Синтаксис импортов
- Поддерживаемые модули
- Примеры

## Конфигурация

### Cargo.toml

**Что:** Конфигурация проекта

**Важные параметры:**
```toml
[package]
name = "backenium_translator"  # Имя библиотеки
version = "0.1.3"

[lib]
name = "backenium_translator"  # Имя lib кратера
path = "src/lib.rs"

[[bin]]
name = "bkm_translator"        # Имя бинарника
path = "src/main.rs"
```

## Примеры .bkc файлов

### helloworld.bkc
Простой пример с одной функцией

### test_simple.bkc
Функция добавления

### test_api.bkc
REST API с несколькими маршрутами

### test_import.bkc
Использование импортов

## Чек-лист файлов

При добавлении новой функции проверьте:

- [ ] src/lib.rs - нужен ли экспорт?
- [ ] src/main.rs - нужна ли CLI поддержка?
- [ ] src/modules/mod.rs - добавлен ли новый модуль?
- [ ] src/modules/*/mod.rs - написан ли новый модуль?
- [ ] Cargo.toml - обновлена ли версия?
- [ ] Документация - обновлена ли?

---

**Backenium v0.1.3** - файлы и их роли 📚
