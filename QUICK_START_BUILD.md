# ⚡ Быстрый старт: Krait Build System

## 🚀 За 5 минут до первого приложения

### Шаг 1: Компиляция транслятора (1 минута)

```bash
cd /path/to/krait
cargo build --release
```

Готово! Теперь у вас есть `target/release/krait`

### Шаг 2: Создание проекта (1 минута)

```bash
mkdir my_app
cd my_app

# Создаём необходимую структуру
mkdir krait_src rust_code
```

### Шаг 3: Конфигурация Cargo (1 минута)

Создайте `Cargo.toml`:

```toml
[package]
name = "my_app"
version = "0.1.0"
edition = "2021"

[dependencies]

[[bin]]
name = "my_app"
path = "rust_code/main.rs"
```

### Шаг 4: Пишите код на Krait (2 минуты)

Создайте `krait_src/main.kr`:

```krait
func main()
    print "Hello from Krait!"
end
```

### Шаг 5: Собирайте и запускайте!

```bash
# Одна команда - всё собирается!
krait build

# Запуск
./target/release/my_app

# Результат:
# Hello from Krait!
```

## 📚 Примеры на GitHub

### Пример 1: CLI приложение

```bash
# Смотрите готовый пример
cd test_cli
krait build
./target/debug/krait_cli
```

Это полнофункциональное CLI приложение с командами:
- `ls` - список файлов
- `cat` - содержимое файла
- `help` - справка
- `exit` - выход

### Пример 2: REST API

```bash
cd test_api
krait build
./target/release/krait_api
```

Это REST API сервер с:
- GET /api/users
- POST /api/users
- И ещё 8 маршрутов

## 🎯 Что происходит при `krait build`?

```
┌─────────────────────┐
│  krait_src/*.kr     │  ← Ваш код на Krait
└────────┬────────────┘
         │ (трансляция)
         ↓
┌─────────────────────┐
│  rust_code/*.rs     │  ← Сгенерированный Rust
└────────┬────────────┘
         │ (компиляция)
         ↓
┌─────────────────────┐
│  target/release/    │  ← Финальный бинарник
└─────────────────────┘
```

## 📋 Расширения файлов

| Расширение | Назначение | Статус |
|-----------|-----------|--------|
| `.kr` | Исходный код Krait | ✅ v0.2.0 |
| `.krm` | Модули (планируется) | 🔮 v0.3.0 |

## 🔧 Полезные команды

```bash
# Версия
krait --version

# Справка
krait --help

# Транслировать один файл
krait input.kr output.rs

# Транслировать проект
krait project src_dir out_dir

# Собрать текущий проект
krait build

# Удалить собранные файлы
cargo clean
```

## 💡 Советы

### Совет 1: Структурируйте код

Вместо всего в одном файле:

```
krait_src/
  ├── main.kr      # Entry point
  ├── api.kr       # REST API
  ├── utils.kr     # Вспомогательные функции
  └── handlers.kr  # Обработчики
```

Команда `krait build` автоматически транслирует все!

### Совет 2: Быстрое тестирование

Для быстрого тестирования используйте `--debug`:

```bash
# Быстрая сборка (медленнее работает)
cargo build

# Запуск
./target/debug/my_app

# Или через один шаг
cargo run
```

### Совет 3: Release оптимизация

Для продакшена используйте `--release`:

```bash
# Сборка по умолчанию уже с --release
krait build

# Ищите бинарник в
./target/release/my_app
```

## 🚨 Решение проблем

### Проблема: "command not found: krait"

**Решение:** Используйте полный путь

```bash
/path/to/krait/target/release/krait build
```

или добавьте в PATH:

```bash
export PATH="/path/to/krait/target/release:$PATH"
```

### Проблема: "Cargo.toml not found"

**Решение:** Убедитесь что вы в правильной директории

```bash
# Должен быть Cargo.toml рядом с krait_src/
ls -la

# Результат:
# Cargo.toml
# krait_src/
# rust_code/
```

### Проблема: "directory 'krait_src' not found"

**Решение:** Создайте директорию

```bash
mkdir krait_src
touch krait_src/main.kr
```

## 📖 Дальше

После быстрого старта читайте:
- `BUILD_SYSTEM.md` - Подробное описание системы
- `KRAIT_INTRODUCTION.md` - Введение в язык
- `test_cli/README.md` - Пример CLI приложения
- `test_api/README.md` - Пример REST API

## 🎓 Обучение

### Уровень 1: Hello World (5 мин)
```krait
func main()
    print "Hello, World!"
end
```

### Уровень 2: Функции (15 мин)
```krait
public func greet(txt name) -> txt
    return "Hello, " + name
end

func main()
    txt greeting = greet("Krait")
    print greeting
end
```

### Уровень 3: Условия (30 мин)
```krait
public func is_adult(int age) -> bool
    if age >= 18
        return true
    else
        return false
    end
end

func main()
    if is_adult(25)
        print "Welcome, adult!"
    else
        print "Too young"
    end
end
```

### Уровень 4: REST API (1 час)
```krait
import api from rest

public func get_status() -> txt
    return "ok"
end

@route "/api/status" GET
    txt status = get_status()
    return status
end

func main()
    print "Server running"
end
```

## 🎯 Следующие шаги

1. ✅ Прочитайте этот файл
2. ✅ Выполните примеры
3. 🔄 Создайте свой первый проект
4. 📚 Изучите документацию
5. 💪 Создавайте крутые приложения!

---

## 📞 Помощь

- 📖 **Документация:** Смотрите `.md` файлы в корне проекта
- 🔍 **Примеры:** Смотрите папки `test_cli/` и `test_api/`
- 🐛 **Баги:** Создавайте issues на GitHub
- 💡 **Идеи:** Обсуждайте improvements

---

**Krait v0.2.0** ⚡

Готовы создавать? Давайте начнём! 🚀
