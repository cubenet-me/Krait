# ✅ Поддерживаемые возможности Backenium

## 📊 Статус реализации

### Типы данных ✅
- [x] `int` → `i32`
- [x] `float` → `f32`
- [x] `double` → `f64`
- [x] `txt` → `String`
- [x] `bool` → `bool`
- [x] `auto` → автотипизация

### Функции ✅
- [x] Объявление функций: `func name() ... end`
- [x] С параметрами: `func add(a int, b int) -> int`
- [x] Оба синтаксиса параметров: `type name` и `name type`
- [x] Опциональный возвращаемый тип
- [x] Блоки кода: `{ }` и `end`
- [x] Рекурсия

### Переменные ✅
- [x] Объявление: `int x = 5`
- [x] Без инициализации: `int x`
- [x] Инициализация при объявлении

### Выражения ✅
- [x] Арифметика: `+`, `-`, `*`, `/`, `%`
- [x] Сравнение: `==`, `!=`, `<`, `>`, `<=`, `>=`
- [x] Логика: `&&`, `||`
- [x] Приоритет операций
- [x] Вызовы функций: `func(arg1, arg2)`
- [x] Круглые скобки: `(a + b) * c`

### Управление потоком ✅
- [x] `if (condition) { } else { }`
- [x] Оба синтаксиса блоков: `{ }` и `end`
- [x] Вложенные условия

### Встроенные функции ✅
- [x] `print()` → `println!()`
- [x] `log()` → `eprintln!()`
- [x] `json_encode()` → `serde_json::to_string()`

### Web роуты (базовая) ✅
- [x] `@route "/path" GET { }`
- [x] `@route "/path" POST { }`
- [x] `@route "/path" PUT { }`
- [x] `@route "/path" DELETE { }`
- [x] Регистрация в actix-web

### Комментарии ✅
- [x] Однострочные: `// comment`

---

## 🚧 Планируемые возможности

### Циклы ⏳
- [ ] `while` циклы
- [ ] `for` циклы с диапазонами

### Обработка ошибок ⏳
- [ ] `try ... catch ... end`
- [ ] `raise` с кодами ошибок
- [ ] `error_code`

### Модели данных ⏳
- [ ] `model` объявления
- [ ] ORM интеграция с sea-orm
- [ ] Валидация полей

### Тесты ⏳
- [ ] `test` блоки
- [ ] `assert` проверки
- [ ] Интеграция с cargo test

### Импорты ⏳
- [ ] `import` файлы модулей
- [ ] Модульная система

### Rate-limiting ⏳
- [ ] `@rate = X req/min`
- [ ] Ограничение запросов

### Продвинутые операции ⏳
- [ ] Массивы: `[1, 2, 3]`
- [ ] Словари: `{"key": "value"}`
- [ ] Доступ к полям: `obj.field`
- [ ] Многострочные комментарии: `/* ... */`

---

## 📈 Примеры рабочего кода

### Пример 1: Hello World
```bkc
func main()
    print("Hello World!")
end
```

### Пример 2: Функция с параметрами
```bkc
func add(a int, b int) -> int
    return a + b
end
```

### Пример 3: Условия
```bkc
func max(a int, b int) -> int
    if a > b
        return a
    else
        return b
    end
end
```

### Пример 4: Рекурсия
```bkc
func factorial(n int) -> int
    if n <= 1
        return 1
    else
        return n * factorial(n - 1)
    end
end
```

---

## 🔄 Версия 0.1.0 - Базовая функциональность

Текущая реализация фокусируется на:
1. Основных типах данных
2. Функциях с параметрами
3. Выражениях и операциях
4. Условных операторах
5. Встроенных функциях

**Следующий шаг:** Добавить циклы и обработку ошибок
