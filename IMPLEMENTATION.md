# Backenium to Rust Translator - Полная реализация 🎯

Полнофункциональный **транслятор DSL (Backenium)** в **Rust**, разработанный на Rust с поддержкой синтаксического анализа, генерации кода и создания готовых к компиляции проектов.

## 📌 Обзор проекта

Этот проект демонстрирует:
- 🔤 **Лексический анализ** (tokenization) с поддержкой 40+ токенов
- 🌳 **Синтаксический анализ** (AST construction) с рекурсивным спуском
- 🚀 **Генерацию Rust кода** с правильной типизацией и форматированием
- 🎨 **Полную обработку ошибок** с информативными сообщениями
- 📦 **Создание проектов** с автоматической генерацией `Cargo.toml`

## 🏗️ Архитектура

### Модули кода

```
src/
├── lexer.rs      (450 строк)  - Лексический анализ
├── parser.rs     (400 строк)  - Синтаксический анализ и AST
├── codegen.rs    (280 строк)  - Генерация Rust кода
└── main.rs       (200 строк)  - CLI интерфейс
```

**Всего: ~1300 строк Rust кода**

### Поток данных

```
BKM источник (.bkc)
    ↓
Lexer: tokenization
    ↓
Parser: AST construction
    ↓
CodeGenerator: Rust code
    ↓
Rust файл (.rs) + Cargo.toml
```

## 🔧 Компоненты

### 1. Лексер (lexer.rs)

Преобразует исходный текст в токены:

- **40+ токенов**: ключевые слова, операторы, разделители, литералы
- **Поддержка строк**: с экранированием спецсимволов
- **Комментарии**: строковые комментарии `//`
- **Отслеживание позиций**: для сообщений об ошибках
- **Автоматы**: для чисел, идентификаторов и строк

**Примеры токенов:**
```
Func, End, Return, If, While, For
Int, Float, Double, Txt, Bool
Plus, Minus, Star, Slash, Equal
LeftParen, RightParen, LeftBrace
Identifier("name"), Number("42"), String("hello")
```

### 2. Парсер (parser.rs)

Строит типизированное AST (Abstract Syntax Tree):

**Типы данных (DataType):**
```rust
enum DataType {
    Int,    // → i32
    Float,  // → f32
    Double, // → f64
    Txt,    // → String
    Bool,   // → bool
    Auto,   // → i32 (по умолчанию)
}
```

**Выражения (Expr):**
```rust
enum Expr {
    Literal(String),        // "42", "hello", "true"
    Identifier(String),     // переменные/функции
    BinaryOp { ... },       // a + b, x == y
    FunctionCall { ... },   // func(arg1, arg2)
}
```

**Операторы (Statement):**
```rust
enum Statement {
    VarDecl { ... },    // int x = 5;
    Return(Option<Expr>), // return value;
    If { ... },         // if (cond) { } else { }
    Expression(Expr),   // a + b;
}
```

**Верхний уровень (TopLevel):**
```rust
enum TopLevel {
    Function(FunctionDef),  // func name() -> type { }
    Route(RouteDef),        // @route "/path" GET { }
    Statement(Statement),   // код на верхнем уровне
}
```

**Парсеры:**
- `parse()` - главный парсер программы
- `parse_function()` - функции с параметрами и телом
- `parse_route()` - веб-роуты с HTTP методами
- `parse_statement()` - операторы (переменные, return, if)
- `parse_simple_expr()` - выражения с правильным приоритетом
- `parse_primary_expr()` - литералы и первичные выражения

### 3. Код-генератор (codegen.rs)

Генерирует корректный Rust код из AST:

**Трансформации типов:**
```
BKM Type    → Rust Type
int         → i32
float       → f32
double      → f64
txt         → String
bool        → bool
```

**Генерация кода:**
- Функции с сигнатурами и телом
- Переменные с типами
- Выражения с бинарными операциями
- Условные блоки (if/else)
- Web-роуты с HTTP методами (GET, POST, PUT, DELETE)
- Главная функция `main()` с actix-web HttpServer

**Пример трансформации:**
```
BKM:  func add(int a, int b) -> int { return a + b; }

Rust: fn add(
          a: i32,
          b: i32
      ) -> i32 {
          return (a + b);
      }
```

### 4. CLI интерфейс (main.rs)

**Команды:**

```bash
# Трансляция одного файла
./bkm_translator input.bkc output.rs

# Трансляция проекта
./bkm_translator project /src/dir /out/dir
```

**Функции:**
- `translate_bkm_file()` - трансляция одного файла
- `translate_bkm_project()` - трансляция всего проекта
- Автоматическое создание `Cargo.toml`
- Обработка ошибок с информативными сообщениями

## 📊 Поддерживаемые конструкции

### Типы данных
```
int x = 42;           // целое число
float y = 3.14;       // число с плавающей запятой
double z = 2.718;     // число двойной точности
txt name = "Alice";   // строка
bool flag = true;     // булевое значение
auto value = 100;     // автотипизация
```

### Функции
```
func add(int a, int b) -> int {
    return a + b;
}

func factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
```

### Выражения
```
int result = a + b;           // бинарные операции
bool equal = x == y;          // сравнение
int value = func(arg1, arg2); // вызовы функций
```

### Условные выражения
```
if (condition) {
    // блок if
} else {
    // блок else
}
```

### Web-роуты
```
@route "/api/users" GET {
    // обработка запроса
    return result;
}

@route "/api/data" POST {
    // обработка данных
    return data;
}
```

## 💡 Примеры использования

### Пример 1: Простая функция

**input.bkc:**
```
func add(int a, int b) -> int {
    return a + b;
}
```

**Команда:**
```bash
./bkm_translator input.bkc output.rs
```

**output.rs:**
```rust
fn add(
    a: i32,
    b: i32
) -> i32 {
    return (a + b);
}
```

### Пример 2: Рекурсивная функция

**input.bkc:**
```
func factorial(int n) -> int {
    if (n <= 1) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
```

**output.rs:**
```rust
fn factorial(
    n: i32
) -> i32 {
    if (n <= 1) {
        return 1;
    } else {
        return (n * factorial((n - 1)));
    }
}
```

### Пример 3: Полный проект

**Команда:**
```bash
./bkm_translator project ./my_bkm_project ./my_rust_project
```

**Результат:**
```
my_rust_project/
├── Cargo.toml              (автогенерирован)
└── src/
    ├── main.rs             (автогенерирован)
    ├── routes.rs           (из .bkc файлов)
    └── handlers.rs         (из .bkm файлов)
```

## 🚀 Быстрый старт

### Установка

```bash
git clone <repo>
cd bkm
cargo build --release
```

### Первый запуск

```bash
# Трансляция примера
./target/release/bkm_translator example.bkc output.rs

# Просмотр результата
cat output.rs
```

### Требуемые инструменты

- Rust 1.56+ (для async/await)
- Cargo (встроен в Rust)
- Linux/macOS/Windows (кроссплатформенный)

## 📈 Статистика

| Метрика | Значение |
|---------|----------|
| Строк кода | ~1300 |
| Токенов | 40+ |
| Типов AST | 10+ |
| Операций | 10+ |
| Ключевых слов | 15+ |
| Тестовых файлов | 3 |

## 🎓 Образовательная ценность

Проект демонстрирует:

1. **Структуру компилятора/интерпретатора**
   - Лексический анализ
   - Синтаксический анализ
   - Генерация кода

2. **Парсинг и обработка ошибок**
   - Рекурсивный спуск
   - Приоритет операций
   - Информативные сообщения об ошибках

3. **Код-генерацию**
   - Типизированный вывод
   - Форматирование
   - Модульность

4. **Обработку Rust**
   - Типы данных
   - Функции и параметры
   - Асинхронное программирование

## 🔮 Возможные расширения

### Краткосрочные
- [ ] Полная поддержка try/catch/raise
- [ ] Циклы (while, for)
- [ ] Массивы и коллекции
- [ ] Операции со строками

### Среднесрочные
- [ ] Структуры данных с SeaORM
- [ ] JSON-валидация
- [ ] Документирование функций
- [ ] Юнит-тесты

### Долгосрочные
- [ ] Оптимизация кода
- [ ] Встроенные валидаторы
- [ ] Система модулей
- [ ] Встроенные библиотеки

## 📝 Файлы примеров

### test_simple.bkc
Минимальный пример для отладки

### example.bkc
Пример с функциями и условиями

### full_example.bkc
Полный пример со всеми возможностями

## 🛠️ Тестирование

```bash
# Трансляция всех примеров
./target/release/bkm_translator test_simple.bkc /tmp/test1.rs
./target/release/bkm_translator example.bkc /tmp/test2.rs
./target/release/bkm_translator full_example.bkc /tmp/test3.rs

# Проверка результатов
cat /tmp/test1.rs
cat /tmp/test2.rs
cat /tmp/test3.rs
```

## 📞 Информация

**Язык:** Rust 2021 edition  
**Лицензия:** MIT  
**Версия:** 0.1.0  

---

**Backenium → Rust: От DSL к готовому коду! 🎯**
