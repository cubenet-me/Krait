# 🔗 Система импортов в Backenium (v0.1.2)

## Обзор

Новая система импортов позволяет явно указывать, какие модули и функции вам нужны для работы программы. Это делает код чище и понятнее.

## Синтаксис

```backenium
import name from module
```

Где:
- `name` - имя импортируемого модуля в вашем коде
- `module` - название модуля из стандартной библиотеки

## Поддерживаемые модули

### `rest` - REST API и Web сервер

```backenium
import api from rest
```

**Что генерирует:**
- Импорт `actix_web` только при наличии импорта
- Создание HTTP сервера на `127.0.0.1:8080`
- Поддержка GET, POST, PUT, DELETE методов

**Пример:**
```backenium
import api from rest

@route "/api/status" GET
    return "ok"
end

func main()
    print("Server started")
end
```

**Генерирует Rust код:**
```rust
use actix_web::{web, App, HttpServer, middleware};

#[actix_web::main]
async fn main() -> std::io::Result<()> {
    HttpServer::new(|| {
        App::new()
            .wrap(middleware::Logger::default())
            .route("/api/status", web::get().to(get_api_status))
    })
    .bind("127.0.0.1:8080")?
    .run()
    .await
}
```

### `json` - JSON сериализация

```backenium
import json from json
```

**Что генерирует:**
- Импорт `serde_json`
- Функцию `json_encode()` для кодирования

**Пример:**
```backenium
import json from json

func get_data() -> txt
    txt json_str = json_encode()
    return json_str
end
```

### `db` - База данных (планируется)

```backenium
import database from db
```

**Планируемо для v0.2:**
- Интеграция с SQLx
- Асинхронные операции с БД
- Миграции

## Примеры использования

### Пример 1: Простой REST API

```backenium
import api from rest

func get_users() -> txt
    return "users list"
end

@route "/api/users" GET
    txt result = get_users()
    return result
end

func main()
    print("Server running on http://127.0.0.1:8080")
end
```

### Пример 2: API с JSON

```backenium
import api from rest
import json from json

@route "/api/data" GET
    txt json_response = json_encode()
    return json_response
end

func main()
    print("API started")
end
```

### Пример 3: Множественные импорты

```backenium
import api from rest
import json from json

func prepare_response() -> txt
    return json_encode()
end

@route "/api/response" GET
    txt response = prepare_response()
    return response
end

func main()
    print("Ready")
end
```

## Льготы использования импортов

✅ **Чистота кода** - только нужные импорты в Rust
✅ **Ясность намерений** - видно какие модули используются
✅ **Безопасность** - нет ненужных зависимостей
✅ **Производительность** - меньше кода, быстрее компилируется

## До и после

### Было (без импортов)

```backenium
@route "/api/status" GET
    return "ok"
end

func main()
    print("Server")
end
```

Генерировалось:
```rust
use actix_web::{web, App, HttpServer, HttpResponse, HttpRequest, middleware};
use serde_json::{json, to_string};
use serde::{Deserialize, Serialize};

// ... весь код
```

### Стало (с импортами)

```backenium
import api from rest

@route "/api/status" GET
    return "ok"
end

func main()
    print("Server")
end
```

Генерируется:
```rust
use actix_web::{web, App, HttpServer, middleware};

// ... только нужный код
```

## Импорты и роуты

Если у вас есть `@route` определения, но нет `import api from rest`, транслятор все равно добавит необходимые импорты автоматически для обратной совместимости.

```backenium
// Даже без этого импорта:
// import api from rest

@route "/api/users" GET
    return "users"
end

// Код будет работать, потому что transльтор
// автоматически определит, что нужен REST API
```

## Часто задаваемые вопросы

**Q: Обязательно ли писать импорты?**  
A: Нет, они опциональны. Транслятор автоматически добавит нужные импорты при наличии роутов.

**Q: Что если я напишу импорт, который не использую?**  
A: Импорт все равно будет добавлен в Rust код. Компилятор Rust предупредит об этом.

**Q: Можно ли импортировать несколько модулей с одним именем?**  
A: Нет, имя импорта должно быть уникальным в скопе.

**Q: Какой приоритет: импорты или автоматическое обнаружение?**  
A: Импорты явные, автоматическое обнаружение происходит только если импорта нет.

## Будущие импорты

Планируется добавить в будущих версиях:

- `import db from database` - работа с БД
- `import cache from redis` - кэширование
- `import auth from jwt` - аутентификация
- `import validation from validator` - валидация
- `import middleware from cors` - CORS и другие middleware
- `import templates from template` - шаблоны

## Синтаксис импортов в v0.1.2

```
import identifier from module_name
```

- `identifier` - имя для использования в коде (сейчас не используется, но зарезервировано)
- `module_name` - один из поддерживаемых модулей:
  - `rest` - REST API (actix-web)
  - `json` - JSON работа (serde_json)
  - `db` - база данных (планируется)

---

**Backenium v0.1.2** - Система импортов
