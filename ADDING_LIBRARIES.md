# Руководство: Добавление новых библиотек в Krait

## Быстрый старт

Добавление поддержки новой Rust библиотеки в Krait занимает **3 минуты** и требует изменений только в одном файле.

## Шаг 1: Откройте libs.rs

```bash
nano src/modules/codegen/libs.rs
```

## Шаг 2: Найдите метод `LibraryRegistry::new()`

Это где регистрируются все встроенные библиотеки.

## Шаг 3: Добавьте новую библиотеку

Вот шаблон:

```rust
libs.insert(
    "ID_БИБЛИОТЕКИ".to_string(),
    Library {
        name: "Красивое название".to_string(),
        crate_name: "имя-в-cargo".to_string(),
        imports: vec![
            "use path::to::Module;".to_string(),
            // добавьте столько импортов, сколько нужно
        ],
        features: vec!["категория".to_string()],
    },
);
```

## Пример 1: Добавляем PostgreSQL драйвер

```rust
libs.insert(
    "postgres".to_string(),
    Library {
        name: "PostgreSQL (tokio-postgres)".to_string(),
        crate_name: "tokio-postgres".to_string(),
        imports: vec![
            "use tokio_postgres::Client;".to_string(),
        ],
        features: vec!["database".to_string()],
    },
);
```

Теперь в Krait можно использовать:
```krait
import db from postgres
```

И импорт автоматически добавится в Rust код!

## Пример 2: Добавляем GraphQL

```rust
libs.insert(
    "graphql".to_string(),
    Library {
        name: "GraphQL (async-graphql)".to_string(),
        crate_name: "async-graphql".to_string(),
        imports: vec![
            "use async_graphql::{Schema, Object, Field};".to_string(),
        ],
        features: vec!["api".to_string()],
    },
);
```

## Пример 3: Добавляем ORM (SeaORM)

```rust
libs.insert(
    "seaorm".to_string(),
    Library {
        name: "SeaORM Database ORM".to_string(),
        crate_name: "sea-orm".to_string(),
        imports: vec![
            "use sea_orm::{Database, DbConn, EntityTrait};".to_string(),
        ],
        features: vec!["database".to_string(), "orm".to_string()],
    },
);
```

## Пример 4: Добавляем несколько импортов

```rust
libs.insert(
    "stripe".to_string(),
    Library {
        name: "Stripe Payments".to_string(),
        crate_name: "stripe-rust".to_string(),
        imports: vec![
            "use stripe::Client;".to_string(),
            "use stripe::ChargeParams;".to_string(),
            "use stripe::Charge;".to_string(),
        ],
        features: vec!["payments".to_string()],
    },
);
```

## Как использовать в Krait

После добавления библиотеки:

```krait
import payment from stripe

public func process_payment(amount: int) -> txt
    // твой код с stripe
    return "Payment processed"
end
```

Генератор автоматически добавит:
```rust
use stripe::Client;
use stripe::ChargeParams;
use stripe::Charge;
```

## Популярные библиотеки для добавления

Вот список популярных Rust библиотек, которые можно добавить:

### Web & API
- `axum` - современный web framework
- `rocket` - web framework с macros
- `warp` - асинхронный web framework
- `tonic` - gRPC framework

### Базы данных
- `sqlx` - асинхронный SQL (MySQL, PostgreSQL)
- `mongo` - MongoDB драйвер
- `redis` - Redis клиент
- `cassandra-cpp` - Apache Cassandra

### Аутентификация
- `jsonwebtoken` - JWT токены
- `argon2` - хеширование паролей
- `oauth2` - OAuth 2.0

### Десериализация
- `bincode` - бинарная сериализация
- `protobuf` - Protocol Buffers
- `thrift` - Apache Thrift

### Асинхронность
- `futures` - Future utilities
- `rayon` - параллельные вычисления

### Тестирование
- `tokio-test` - тестирование async кода
- `criterion` - бенчмарки

## Проверка

После добавления библиотеки:

```bash
# Проверить синтаксис
cargo check

# Собрать проект
cargo build

# Создать тестовый файл
cat > test_lib.kr << 'EOF'
import mylib from mylib
EOF

# Транслировать
cargo run -- test_lib.kr
```

## Поиск правильных импортов

1. Откройте https://docs.rs
2. Найдите вашу библиотеку
3. Посмотрите примеры использования
4. Скопируйте нужные `use` строки

Например для `tokio-postgres`:
```
https://docs.rs/tokio-postgres/0.7.8/tokio_postgres/

use tokio_postgres::Client;
use tokio_postgres::NoTls;
```

## Проблемы и решения

### Проблема: "невозможно найти крейт"

Убедитесь, что:
1. Название в `crate_name` совпадает с именем в cargo.io
2. Вы правильно скопировали импорты

### Проблема: "импорт не работает"

Возможные причины:
1. Неправильный путь импорта (см. docs.rs)
2. Нужно добавить features в Cargo.toml
3. Версия библиотеки устарела

### Решение

Проверьте документацию на docs.rs и сравните с примерами.

## FAQ

**Q: Могу ли я добавить библиотеку с условиями?**  
A: Пока нет, но это запланировано. Сейчас все импорты добавляются всегда.

**Q: Нужно ли обновлять Cargo.toml?**  
A: Нет, это делается автоматически при сборке генерируемого кода.

**Q: Можно ли удалить библиотеку?**  
A: Да, просто удалите `libs.insert(...)` блок.

**Q: Как переименовать существующую?**  
A: Измените строку `"old_id"` на `"new_id"`.

## Лучшие практики

✅ **Делайте:**
- Добавляйте понятные имена библиотек
- Проверяйте документацию на docs.rs
- Тестируйте импорты в отдельном проекте

❌ **Не делайте:**
- Не добавляйте неработающие импорты
- Не копируйте случайные коды из интернета
- Не забывайте проверять версии

## Примеры правильных добавлений

### ✅ Хорошо

```rust
libs.insert(
    "mysql".to_string(),
    Library {
        name: "MySQL (sqlx)".to_string(),
        crate_name: "sqlx".to_string(),
        imports: vec![
            "use sqlx::mysql::MySqlPool;".to_string(),
        ],
        features: vec!["database".to_string()],
    },
);
```

### ❌ Плохо

```rust
libs.insert(
    "123invalid".to_string(),  // ❌ неверное имя
    Library {
        name: "some random lib".to_string(),
        crate_name: "crate-that-doesnt-exist".to_string(),  // ❌ крейт не существует
        imports: vec![
            "use random::invalid::path;".to_string(),  // ❌ импорт не существует
        ],
        features: vec![],
    },
);
```

## Контрибьютинг

Если вы добавили полезную библиотеку:

1. Протестируйте её
2. Создайте pull request
3. Опишите в сообщении, какие примеры работают

Спасибо за контрибьютинг в Krait! 🎉

---

**Нужна помощь?** Откройте issue на GitHub или спросите в дискорде.
