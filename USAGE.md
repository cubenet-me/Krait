# Руководство пользователя - Backenium Translator

## 📖 Содержание

1. [Установка](#установка)
2. [Базовое использование](#базовое-использование)
3. [Синтаксис Backenium](#синтаксис-backenium)
4. [Примеры](#примеры)
5. [Решение проблем](#решение-проблем)

## 🔧 Установка

### Требования
- Rust 1.56+
- Cargo
- 100 МБ свободного места

### Шаги установки

```bash
# 1. Клонируем репозиторий
git clone <repo-url>
cd bkm

# 2. Компилируем в режиме релиза
cargo build --release

# 3. Проверяем установку
./target/release/bkm_translator --help
# или просто
./target/release/bkm_translator
```

## 🚀 Базовое использование

### Трансляция одного файла

```bash
./target/release/bkm_translator input.bkc output.rs
```

**Параметры:**
- `input.bkc` - исходный файл Backenium
- `output.rs` - выходной файл Rust

**Примеры:**
```bash
./target/release/bkm_translator example.bkc generated.rs
./target/release/bkm_translator my_code.bkm handlers.rs
```

### Трансляция проекта

```bash
./target/release/bkm_translator project /path/to/bkm/project /path/to/rust/project
```

**Параметры:**
- `project` - ключевое слово для трансляции проекта
- `/path/to/bkm/project` - директория с .bkc/.bkm файлами
- `/path/to/rust/project` - целевая директория для результата

**Результат:**
```
rust_project/
├── Cargo.toml
├── src/
│   ├── main.rs
│   ├── handlers.rs
│   └── routes.rs
└── target/
```

## 📚 Синтаксис Backenium

### Типы данных

```
int      - целые числа (i32 в Rust)
float    - числа с плавающей запятой (f32)
double   - числа двойной точности (f64)
txt      - строки (String)
bool     - булевы значения
auto     - автотипизация
```

### Переменные

```
// Объявление с инициализацией
int count = 42;
float pi = 3.14;
txt greeting = "Hello";
bool active = true;

// Объявление без инициализации
int x;
float y;
```

### Функции

```
func function_name(type param1, type param2, ...) -> return_type {
    // тело функции
    return value;
}
```

**Пример:**
```
func multiply(int a, int b) -> int {
    return a * b;
}
```

### Выражения

```
// Арифметические операции
int sum = a + b;
int diff = a - b;
int prod = a * b;
int div = a / b;
int rem = a % b;

// Сравнение
bool eq = a == b;
bool ne = a != b;
bool lt = a < b;
bool gt = a > b;
bool le = a <= b;
bool ge = a >= b;

// Логические операции
bool and = a && b;
bool or = a || b;
bool not = !a;
```

### Условные выражения

```
if (condition) {
    // блок then
} else {
    // блок else
}
```

**Пример:**
```
if (age >= 18) {
    txt status = "adult";
} else {
    txt status = "minor";
}
```

### Web роуты (экспериментально)

```
@route "/path/to/resource" METHOD {
    // код обработки
    return response;
}
```

**Поддерживаемые методы:**
- GET - получение данных
- POST - создание/обновление
- PUT - полное обновление
- DELETE - удаление

**Пример:**
```
@route "/api/users" GET {
    return "list of users";
}

@route "/api/users" POST {
    int id = 1;
    txt name = "John";
    return id;
}
```

## 💡 Примеры

### Пример 1: Простая программа

**math.bkc:**
```
func square(int x) -> int {
    return x * x;
}

func is_positive(int n) -> bool {
    return n > 0;
}
```

**Команда:**
```bash
./bkm_translator math.bkc math_generated.rs
```

**Результат (math_generated.rs):**
```rust
fn square(
    x: i32
) -> i32 {
    return (x * x);
}

fn is_positive(
    n: i32
) -> bool {
    return (n > 0);
}
```

### Пример 2: Функция с условиями

**logic.bkc:**
```
func max(int a, int b) -> int {
    if (a > b) {
        return a;
    } else {
        return b;
    }
}

func abs(int x) -> int {
    if (x < 0) {
        return 0 - x;
    } else {
        return x;
    }
}
```

**Трансляция:**
```bash
./bkm_translator logic.bkc logic_generated.rs
```

### Пример 3: Рекурсивные функции

**recursion.bkc:**
```
func fibonacci(int n) -> int {
    if (n <= 1) {
        return n;
    } else {
        return fibonacci(n - 1) + fibonacci(n - 2);
    }
}
```

### Пример 4: Web API

**api.bkc:**
```
@route "/api/calculate" POST {
    int x = 10;
    int y = 20;
    int result = x + y;
    return result;
}

@route "/api/status" GET {
    txt status = "OK";
    return status;
}
```

## 🐛 Решение проблем

### Ошибка: "Expected RightBrace"

**Причина:** Неправильное закрытие блока кода

**Решение:**
```
// Неправильно:
func test() -> int {
    return 42;
// Правильно:
func test() -> int {
    return 42;
}
```

### Ошибка: "Expected type"

**Причина:** Пропущена типизация переменной

**Решение:**
```
// Неправильно:
x = 42;
// Правильно:
int x = 42;
```

### Ошибка: "Unexpected token"

**Причина:** Недопустимая конструкция

**Решение:** Проверьте синтаксис согласно документации

## 📊 Результаты трансляции

### Типы данных

| BKM | Rust | Пример |
|-----|------|--------|
| int | i32 | 42 |
| float | f32 | 3.14 |
| double | f64 | 2.718 |
| txt | String | "hello" |
| bool | bool | true |

### Выражения

| BKM | Rust |
|-----|------|
| a + b | (a + b) |
| a == b | (a == b) |
| a && b | (a && b) |
| func(x) | func(x) |

## ✅ Чек-лист перед использованием

- [ ] Установлен Rust 1.56+
- [ ] Скомпилирован транслятор (`cargo build --release`)
- [ ] Есть файлы .bkc/.bkm для трансляции
- [ ] Проверена синтаксис Backenium
- [ ] Задана правильная целевая директория

## 🔗 Полезные ссылки

- [Документация по реализации](IMPLEMENTATION.md)
- [Основной README](README.md)
- [Примеры файлов](example.bkc)

---

**Удачи с трансляцией Backenium кода! 🎉**
