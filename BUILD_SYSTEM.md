# 🐍 Krait v0.2.0 - Build System & Extensions

## 📋 Обновления в v0.2.0

### 1. Новая команда: `krait build`

Команда `krait build` автоматизирует весь процесс:
1. ✅ Поиск всех `.kr` файлов в папке `krait_src/`
2. ✅ Трансляция их в Rust (`rust_code/`)
3. ✅ Компиляция проекта с помощью `cargo build --release`

#### Использование

```bash
cd your_krait_project
krait build
```

#### Пример вывода

```
🔨 Building Krait project...

📦 Step 1: Translating Krait files...

  ✓ main.kr → main.rs
  ✓ utils.kr → utils.rs
  ✓ api.kr → api.rs

✅ Транслировано: 3 файлов

🦀 Step 2: Compiling with Cargo...

   Compiling krait_app v0.1.0
   Finished `release` profile [optimized] target(s) in 2.34s

✅ Compilation successful!
✓ Проект успешно собран
```

### 2. Новые расширения файлов

Krait теперь поддерживает два расширения:

#### `.kr` - Исходный код (Krait Code)

Основной язык для разработки. Используется для:
- Основной логики приложения
- REST API хендлеров
- Бизнес-логики

**Пример: `main.kr`**
```krait
import api from rest

public func get_users() -> txt
    return "[{\"id\": 1, \"name\": \"Alice\"}]"
end

@route "/api/users" GET
    txt users = get_users()
    return users
end

func main()
    print "Server started"
end
```

#### `.krm` - Модули (Krait Module)

Расширение для модульного кода (планируется в v0.3.0):
- Переиспользуемые компоненты
- Библиотеки
- Утилиты

**Пример: `utils.krm`** (планируется)
```krait
public func format_json(txt data) -> txt
    return data
end

public func parse_json(txt json) -> auto
    return 1
end
```

### 3. Структура проекта с `krait build`

```
my_project/
│
├── Cargo.toml                      # Конфигурация Cargo
│
├── krait_src/                      # Исходники на Krait
│   ├── main.kr                     # Главный файл
│   ├── api.kr                      # REST API логика
│   ├── models.kr                   # Структуры данных
│   ├── handlers.kr                 # Обработчики
│   └── utils.kr                    # Утилиты
│
├── rust_code/                      # Генерируемые Rust файлы
│   ├── main.rs                     # → main.kr
│   ├── api.rs                      # → api.kr
│   ├── models.rs                   # → models.kr
│   ├── handlers.rs                 # → handlers.kr
│   └── utils.rs                    # → utils.kr
│
├── src/
│   ├── lib.rs                      # Могут быть кастомные модули
│   └── bin/
│       └── custom.rs               # Кастомная логика если нужна
│
└── target/
    └── release/
        └── my_project              # Финальный бинарник
```

#### Как это работает:

1. **Разработка на Krait:**
   ```bash
   # Создаёте/редактируете файлы в krait_src/
   vim krait_src/main.kr
   vim krait_src/api.kr
   ```

2. **Одна команда - всё собирается:**
   ```bash
   krait build
   ```
   
   Она:
   - Ищет все `.kr` файлы в `krait_src/`
   - Транслирует их в `rust_code/`
   - Автоматически запускает `cargo build --release`
   - Создаёт готовый бинарник в `target/release/`

3. **Запуск приложения:**
   ```bash
   ./target/release/my_project
   ```

## 🔄 Полный цикл разработки

### 1️⃣ Инициализация проекта

```bash
mkdir my_krait_app
cd my_krait_app

# Создаём нужные директории
mkdir krait_src rust_code

# Создаём Cargo.toml
cat > Cargo.toml << EOF
[package]
name = "my_krait_app"
version = "0.1.0"
edition = "2021"

[dependencies]

[[bin]]
name = "my_app"
path = "rust_code/main.rs"
EOF
```

### 2️⃣ Разработка на Krait

```krait
# krait_src/main.kr
func main()
    print "Hello from Krait!"
end
```

### 3️⃣ Сборка проекта

```bash
# Переходим в директорию проекта
cd my_krait_app

# Одна команда - и всё собирается!
krait build

# Или если есть несколько файлов:
# - main.kr
# - api.kr
# - utils.kr
# - Все они автоматически трансляцируются и компилируются
```

### 4️⃣ Запуск

```bash
./target/release/my_app
# Hello from Krait!
```

## 📊 Примеры проектов

### ✅ Пример 1: Простой CLI (test_cli/)

```bash
cd test_cli
krait build
./target/debug/krait_cli

# 🐍 Krait CLI Application
# Type 'help' for commands
# ...
```

**Структура:**
- `krait_src/main.kr` - CLI логика
- `rust_code/main.rs` - сгенерировано
- Компилируется в `target/debug/krait_cli`

### ✅ Пример 2: REST API (test_api/)

```bash
cd test_api
krait build
./target/release/krait_api

# Слушает на http://127.0.0.1:8080
```

**Структуры:**
- `krait_src/api.kr` - REST API маршруты
- `krait_src/handlers.kr` - обработчики
- Компилируется в`target/release/krait_api`

## 🚀 Команды Krait Translator v0.2.0

| Команда | Описание |
|---------|---------|
| `krait --help` | Показать справку |
| `krait --version` | Показать версию |
| `krait input.kr output.rs` | Транслировать файл |
| `krait project dir1 dir2` | Транслировать проект |
| `krait build` | Транслировать + скомпилировать |

## 🎯 Преимущества `krait build`

✨ **Простота** - Одна команда для всего
✨ **Автоматизация** - Не нужно помнить командирует
✨ **Скорость** - Быстрая разработка и тестирование
✨ **Удобство** - Как `npm run build` или `cargo build`
✨ **Масштабируемость** - Работает с несколькими файлами

## 📝 Расширение файлов: .kr vs .krm

### .kr - Code (Основной исходник)

**Использование:**
- Основная логика приложения
- REST API хендлеры
- Бизнес-логика

**При сборке:**
```bash
krait_src/*.kr → rust_code/*.rs
```

**Пример проекта:**
```
krait_src/
  ├── main.kr      # Entry point
  ├── api.kr       # API routes
  └── db.kr        # Database logic
```

### .krm - Module (Модули, v0.3.0+)

**Использование (планируется):**
- Переиспользуемые компоненты
- Вспомогательные функции
- Конфигурация

**При сборке:**
```bash
krait_src/*.krm → rust_code/*.rs (в отдельном модуле)
```

**Пример структуры (v0.3.0):**
```
krait_src/
  ├── main.kr
  └── modules/
      ├── utils.krm    # Утилиты
      ├── config.krm   # Конфиг
      └── db.krm       # БД логика
```

## ✅ Чек-лист для нового проекта

Чтобы начать новый проект на Krait:

- [ ] Создать директорию проекта
- [ ] Создать `Cargo.toml` с конфигурацией
- [ ] Создать `krait_src/` директорию
- [ ] Создать `krait_src/main.kr` файл
- [ ] Создать `rust_code/` директорию (или будет создана автоматически)
- [ ] Запустить `krait build`
- [ ] Готово! 🎉

## 🔮 Будущее (v0.3.0+)

- [ ] Поддержка `.krm` модулей
- [ ] Watch mode (`krait build --watch`)
- [ ] Кастомизация output пути
- [ ] IDE интеграция
- [ ] REPL для интерактивного тестирования

---

**Krait v0.2.0**
- ✅ Команда `krait build`
- ✅ Расширения `.kr`
- 🎯 Расширения `.krm` (v0.3.0)

Быстро. Безопасно. Просто. 🐍⚡
