# 🐍 Krait - Быстрый и Безопасный Язык программирования

## Что такое Krait?

**Krait** - это высокоуровневый язык программирования, который компилируется в **Rust**, что обеспечивает:

- 🚀 **Скорость** Python с производительностью Rust
- 🔒 **Безопасность** гарантируемая Rust компилятором
- 📝 **Простота** Синтаксис похож на Python, но type-safe
- 🎯 **Web-First** Встроенная поддержка REST API

## История переименования

| Версия | Название    | Статус |
|--------|-------------|--------|
| v0.1.x | Backenium   | ❌ Архивирована |
| v0.2.0 | Krait       | ✅ Текущая |

**Почему Krait?**
- **Крайт** (англ. Krait) - ядовитая змея, известная своей **скоростью и точностью**
- Символизирует **быстроту** выполнения и **безопасность** языка
- Аналогия с Python (змея) но более **мощная и опасная** (быстрая и безопасная)

## Основные характеристики

### Синтаксис как Python
```krait
public func greet(txt name) -> txt
    return "Hello, " + name
end
```

### Производительность как Rust
```rust
// Генерируется идиоматичный Rust код
pub fn greet(name: String) -> String {
    return format!("Hello, {}", name);
}
```

### Type Safety
```krait
int count = 5           // Type: i32
txt message = "Hello"   // Type: String
bool active = true      // Type: bool
float price = 3.14      // Type: f64
```

## Типы данных

| Krait  | Rust     | Описание |
|--------|----------|---------|
| int    | i32      | Целые числа |
| float  | f64      | Числа с плавающей точкой |
| txt    | String   | Текстовые строки |
| bool   | bool     | Логические значения |
| auto   | i32      | Автоматический тип |

## REST API поддержка

```krait
import api from rest

@route "/api/users" GET
    int count = get_user_count()
    return count
end

@route "/api/users" POST
    int new_id = 3
    return new_id
end
```

Генерирует полноценный async Web сервер на actix-web!

## Управление видимостью

```krait
public func public_function()
    // Доступна везде
end

private func private_function()
    // Доступна только внутри модуля
end

func default_function()
    // По умолчанию приватная
end
```

## Условные выражения

```krait
if x > 5
    print "big"
else
    if x > 0
        print "small"
    else
        print "zero"
    end
end
```

## Функции и параметры

```krait
public func calculate(int a, int b) -> int
    int result = a + b
    return result
end

public func greet(txt name) -> txt
    return "Hello, " + name
end
```

## Примеры использования

### Простой Hello World
```krait
func main()
    print "Hello, World!"
end
```

### Функция с параметрами
```krait
public func add(int a, int b) -> int
    return a + b
end

func main()
    int result = add(5, 3)
    print result
end
```

### REST API endpoint
```krait
import api from rest

public func get_user_name(int id) -> txt
    if id == 1
        return "Alice"
    else
        return "Unknown"
    end
end

@route "/api/user/1" GET
    txt name = get_user_name(1)
    return name
end
```

## Версия и команды

### Проверить версию
```bash
krait --version
# Krait Translator v0.2.0
```

### Транслировать файл
```bash
krait input.kr output.rs
```

### Транслировать проект
```bash
krait project src_dir output_dir
```

## Расширение файлов

- `.kr` - исходный код на Krait (Krait source)
- `.rs` - сгенерированный Rust код

## Сравнение с другими языками

| Язык   | Скорость | Безопасность | Простота | Web API |
|--------|----------|--------------|----------|---------|
| Python | ⭐⭐    | ⭐           | ⭐⭐⭐  | ⭐⭐   |
| Rust   | ⭐⭐⭐  | ⭐⭐⭐      | ⭐      | ⭐⭐⭐ |
| Krait  | ⭐⭐⭐  | ⭐⭐⭐      | ⭐⭐⭐  | ⭐⭐⭐ |

## Преимущества Krait

✨ **Скорость Python** - Простой для изучения синтаксис
✨ **Безопасность Rust** - Компилятор предотвращает ошибки
✨ **Production Ready** - Генерирует production-grade код
✨ **Web-first** - Встроенная поддержка REST API
✨ **Type-safe** - Все типы проверяются на этапе трансляции
✨ **Zero overhead** - Компилируется в оптимизированный Rust

## Использование в проектах

### Простой скрипт
```bash
# write script.kr
krait script.kr script.rs
# Use in your Rust project
```

### Web API
```bash
# write api.kr
krait api.kr api.rs
# cargo build && cargo run
```

### Проектный структура
```bash
krait project ./krait_src ./rust_src
```

## Требования

- Rust toolchain (для компиляции сгенерированного кода)
- Krait translator (текущий проект)

## Установка

```bash
git clone <repo>
cd krait
cargo build --release
./target/release/krait --version
```

## Документация

Полная документация находится в:
- `README.md` - описание проекта
- `test_api/` - примеры REST API
- `VERSION_0_1_4.md` - генерация тела хендлеров
- `RELEASE_v0_1_4.md` - информация о версии

## Дорожная карта (Roadmap)

### v0.2.0 (текущая)
- ✅ Парсинг и генерация
- ✅ REST API поддержка
- ✅ Типизация переменных
- ✅ Условные выражения

### v0.3.0 (планируется)
- [ ] Параметры маршрутов (`:id`)
- [ ] Query параметры
- [ ] Request body парсинг
- [ ] Error handling
- [ ] Логирование

### v0.4.0 (планируется)
- [ ] Классы и структуры
- [ ] Наследование
- [ ] Interfaces/Traits
- [ ] Модули и пакеты

### v1.0.0 (планируется)
- [ ] REPL интерпретатор
- [ ] LSP поддержка
- [ ] IDE расширения
- [ ] Package manager

## Примеры проектов

В папке `test_api/` находятся полные примеры:
- `api.kr` - простая версия с 10 маршрутами
- `api_full.kr` - полная версия с функциями логики
- `api.rs`, `api_full.rs` - сгенерированный код

## Заключение

**Krait** - это мост между простотой Python и мощью Rust. Он позволяет писать код на простом языке и получать production-ready высокопроизводительные приложения.

Быстро. Безопасно. Просто.

---

**Krait v0.2.0** - Пусть начнется новая эра программирования! 🐍⚡

